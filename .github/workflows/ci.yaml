name: mappshow ci
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run ruff-check
        uses: jpetrucciani/ruff-check@main
        with:
          path: 'mappshow/backend'


